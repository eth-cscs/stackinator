spack:
  include:
  - packages.yaml
  - config.yaml
  - compilers.yaml
  specs:
{% for spec in config.specs %}
  - {{ spec }}
{% endfor %}
  view: false
  config:
    deprecated: {{ config.deprecated }}
  concretizer:
    unify: when_possible
    reuse: false

