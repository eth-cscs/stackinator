full-env:
  compiler: [gcc]
  unify: when_possible
  specs:
  - osu-micro-benchmarks@5.9
  - hdf5 +mpi
  network:
    mpi: cray-mpich +cuda
    specs: ["libfabric@2.2.0"]
  packages:
  - perl
  - git
  variants:
  - +mpi
  - +cuda
  views:
    default:
defaults-env:
  compiler: [gcc]
  specs:
  - tree
  # assert variants=[]
  # assert unify=True
  # assert network={"mpi": None, "specs": []}
  # assert packages=[]
  # assert view=True
defaults-env-mpi-nogpu:
  compiler: [gcc]
  specs:
  - tree
  network:
    mpi: cray-mpich
