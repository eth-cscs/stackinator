mpi:
  compiler:
      - toolchain: gcc
        spec: gcc@12
  unify: true
  specs:
  - cuda@11.8
  - osu-micro-benchmarks@5.9
  mpi:
    spec: cray-mpich-binary
    gpu: cuda
tools:
  compiler:
      - toolchain: gcc
        spec: gcc@12
  unify: true
  specs:
  - cmake
