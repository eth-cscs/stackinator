packages:
    gcc-env:
      compiler:
          toolchain: gcc
          spec: gcc@11.3.0
      unify: true
      specs:
      - osu-micro-benchmarks@5.9
      - hdf5 +mpi
      mpi: cray-mpich-binary
      gpu: false
      #gpu: cuda,rocm
    tools:
      compiler:
          toolchain: gcc
          spec: gcc@11.3.0
      unify: true
      specs:
      - cmake
      mpi: false
      gpu: false
